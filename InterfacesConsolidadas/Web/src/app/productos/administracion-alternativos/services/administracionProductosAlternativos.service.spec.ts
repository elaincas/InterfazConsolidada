/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AdministracionProductosAlternativosService } from './administracionProductosAlternativos.service';

describe('Service: AdministracionProductosAlternativos', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AdministracionProductosAlternativosService]
    });
  });

  it('should ...', inject([AdministracionProductosAlternativosService], (service: AdministracionProductosAlternativosService) => {
    expect(service).toBeTruthy();
  }));
});
